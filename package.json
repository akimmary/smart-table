{
  "name": "canonical",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"no tests\" && exit 0"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
